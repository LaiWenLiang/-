#ifndef __APP_OTA_H
#define __APP_OTA_H

/***************************************************************************************************
 * APP 侧 OTA 升级接收模块（RS485 通道，USART1 复用）
 * 上位机通过 RS485 发固件 -> 存 W25Q32 暂存区 -> 校验 -> 写启动标志 -> 复位交给 Bootloader
 ***************************************************************************************************/

/* 初始化 W25Q32 / W24C02（在 APP_Tasks_Create 里调用） */
void App_Ota_Init(void);

/* OTA 接收任务（FreeRTOS 任务函数），含"新 APP 首次启动确认" */
void App_Ota_Task(void *param);

#endif /* __APP_OTA_H */
